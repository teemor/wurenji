# wurenji

Layui single-page drone control big screen.

## Preview

Open `index.html` directly in a browser.

The page uses the renamed slices from `assets/images` and local fonts from `assets/fonts`.
